{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ledgerly.dev/contracts/embed-query-request.schema.json",
  "title": "EmbedQueryRequest",
  "description": "The full request body of `POST /embed-query` — plain JSON, unlike the multipart document endpoints, since this carries text only.",
  "type": "object",
  "additionalProperties": false,
  "required": ["text"],
  "properties": {
    "text": {
      "description": "The text to embed, e.g. the extracted expense fields rendered as a short description, for a pgvector nearest-neighbor search against `policy_chunk`.",
      "type": "string",
      "minLength": 1
    },
    "correlation_id": {
      "type": ["string", "null"],
      "maxLength": 128
    }
  }
}
